---
name: test
description: Write or update tests for the target code, then run them.
intent: planning
---

The user invoked the /test command: the deliverable is tests. Read the target
code first, follow the project's existing test layout, framework, and naming
conventions, then write or update the tests. Run them afterwards and report
the results, including any failures left unresolved.
