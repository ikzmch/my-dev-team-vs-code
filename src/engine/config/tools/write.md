---
name: write
sideEffecting: false
previewArg: path
snippetArg: contents
---

Create or overwrite a file. In a multi-root workspace, prefix the path with its
folder's name (e.g. backend/src/app.ts), exactly as the search tool lists it.
